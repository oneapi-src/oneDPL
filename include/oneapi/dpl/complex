// -*- C++ -*-
//===----------------------------------------------------------------------===//
//
// Copyright (C) Intel Corporation
//
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
//
// This file incorporates work covered by the following copyright and permission
// notice:
//
// Part of the LLVM Project, under the Apache License v2.0 with LLVM Exceptions.
// See https://llvm.org/LICENSE.txt for license information.
//
//===----------------------------------------------------------------------===//

#ifndef __ONEDPL_complex
#define __ONEDPL_complex

#include "oneapi/dpl/internal/common_config.h"
#include <complex>

namespace oneapi
{
namespace dpl
{
using ::std::complex;

using ::std::arg;
using ::std::conj;
using ::std::exp;
using ::std::imag;
using ::std::norm;
using ::std::polar;
using ::std::proj;
using ::std::real;

template <class T>
T
abs(const complex<T>& z)
{
    return ::std::abs(z);
}

template <class T>
complex<T>
acos(const complex<T>& z)
{
    return ::std::acos(z);
}

template <class T>
complex<T>
acosh(const complex<T>& z)
{
    return ::std::acosh(z);
}

template <class T>
complex<T>
cos(const complex<T>& z)
{
    return ::std::cos(z);
}

template <class T>
complex<T>
cosh(const complex<T>& z)
{
    return ::std::cosh(z);
}

template <class T>
complex<T>
pow(const complex<T>& x, const complex<T>& y)
{
    return ::std::pow(x, y);
}

template <class T>
complex<T>
pow(const complex<T>& x, const T& y)
{
    return ::std::pow(x, y);
}

template <class T>
complex<T>
pow(const T& x, const complex<T>& y)
{
    return ::std::pow(x, y);
}

template <class T>
dpl::complex<T>
sin(const dpl::complex<T>& z)
{
    return ::std::sin(z);
}

template <class T>
dpl::complex<T>
tan(const dpl::complex<T>& z)
{
    return ::std::tan(z);
}

template <class T>
dpl::complex<T>
tanh(const dpl::complex<T>& z)
{
    return ::std::tanh(z);
}

} // namespace dpl
} // namespace oneapi

namespace dpl = oneapi::dpl;

#endif // __ONEDPL_complex
